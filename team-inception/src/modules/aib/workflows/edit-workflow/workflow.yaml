# Edit Workflow - Workflow Editor Configuration
name: "edit-workflow"
description: "Edit existing AIRchetipo workflows while following all best practices and conventions"
author: "AIRchetipo"

# Critical variables load from config_source
config_source: "{project-root}/{air_folder}/aib/config.yaml"
communication_language: "{config_source}:communication_language"
user_name: "{config_source}:user_name"

# Required Data Files - Critical for understanding workflow conventions
workflow_creation_guide: "{project-root}/{air_folder}/aib/workflows/create-workflow/workflow-creation-guide.md"
workflow_execution_engine: "{project-root}/{air_folder}/core/tasks/workflow.xml"

# Optional docs that can be used to understand the target workflow
recommended_inputs:
  - target_workflow: "Path to the workflow.yaml file to edit"
  - workflow_examples: "{project-root}/{air_folder}/aim/workflows/"

# Module path and component files
installed_path: "{project-root}/{air_folder}/aib/workflows/edit-workflow"
template: false # This is an action workflow - no template needed
instructions: "{installed_path}/instructions.md"
validation: "{installed_path}/checklist.md"

standalone: true

# Web bundle configuration
web_bundle: false # AIB workflows run locally in AIRchetipo project
