---
description: Initialize the backlog from the PRD using the Analyst agent
agent: analyst
subtask: true
---
Read `@docs/prd.md` and build the initial backlog using only that document as input. Write the output to `docs/backlog.yaml` (copy it from `.opencode/templates/backlog.yaml` first if it does not exist) and **do not touch** any other files.
