---
description: Collaboratively create or refine a user story with the Analyst agent
agent: analyst
subtask: true
---
Treat `$arguments` as the user’s initial idea for a new story (or refinement request). Interview the user via clarifying questions and lightweight brainstorming to converge on persona, need, value, KPIs, and constraints before finalizing the backlog item. Use `@docs/prd.md` as the product vision touchstone to challenge or enrich the idea, yet remain free to encode intents that extend beyond the PRD when the user requests them. Update only `docs/backlog.yaml` (initializing it from `.opencode/templates/backlog.yaml` if missing) with any new/updated epics, stories, Gherkin acceptance criteria (≥3 scenarios), and 3–7 linked tasks per story while preserving traceability per the analyst playbook. Do not modify other files.
